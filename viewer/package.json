{
    "name": "protovoice-viewer",
    "version": "1.0.0",
    "description": "",
    "directories": {
        "test": "test"
    },
    "targets": {
        "global": {
            "context": "browser",
            "outputFormat": "global",
            "source": "prod/pv-viewer.js"
        },
        "module": {
            "outputFormat": "esmodule",
            "source": "prod/module.js"
        },
        "css": {
            "source": "prod/pv-style.css"
        }
    },
    "scripts": {
        "build": "spago build",
        "test": "spago test",
        "serve": "parcel dev/index.html --open",
        "build-prod": "rm -rf dist prod && mkdir -p prod && cp dev/pv-viewer.js prod/ && cp dev/css/pv-style.css prod/ && spago bundle-module --to prod/module.js && parcel build --no-cache"
    },
    "dependencies": {
        "vexflow": "^3.0.9"
    },
    "devDependencies": {
        "express": "^4.17.1",
        "parcel": "^2.0.0",
        "purescript": "^0.14.4",
        "spago": "^0.20.3"
    }
}
